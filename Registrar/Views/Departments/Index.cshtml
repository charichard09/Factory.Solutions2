@{
  Layout = "_Layout";
}

<h1>Department</h1>

@using Registrar.Models

@if (Model.Count == 0)
{
  <p>No Departments have been added</p>
}

<p class="enlarge">@Html.ActionLink("Add new department")</p>